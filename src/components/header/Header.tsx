import { FC } from 'react'
import  "./header.style.scss";

const Header: FC = () => {
    return <header>
        <div className="container">
        <h1 className="logo">P_CHERRY</h1>
        </div>
    </header>
}

export default Header
